describe <%= class_name %>, '#<%= method_name %>' do
<% flow_tree.flows.each do |flow| -%>
  it '<%= flow.description %>' do
    <%= flow.instance_name %> = <%= flow.class_name %>.new

    <%= flow.instance_name %>.<%= flow.method_call %>.should == <%= flow.return_value %>
  end
<% end -%>
end
